package com.spendsms.app.data

/**
 * Data layer boundary.
 *
 * Implements repositories and adapters for SMS source, Room, parser bundles,
 * remote config, telemetry, and support submission. May depend on domain
 * interfaces and platform infrastructure.
 *
 * Room entities, DAOs, and network clients are intentionally deferred.
 */
object DataLayer
