{
  "name": "spendsms-phase0-infra",
  "version": "0.1.0",
  "private": true,
  "description": "SpendSMS Phase-0 AWS CDK control/observability plane. Synth/validate only in this prompt — do not deploy.",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "jest",
    "synth": "cdk synth --quiet --all",
    "validate": "tsc --noEmit && jest && cdk synth --quiet --all"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.10",
    "aws-cdk": "2.178.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.178.2",
    "constructs": "^10.4.2"
  }
}
